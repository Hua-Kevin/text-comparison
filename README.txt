Text Comparison App
  This is a simple text comparison application developed using Qt Creator. It allows users to compare two text files and displays the differences between them.

Features
  Load two text files for comparison
  Highlight differences between the two files
  Navigation buttons to move between differences
  Word-level and line-level comparison options
  Save comparison results to a new file
